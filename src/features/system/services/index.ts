import { mockSystemService } from '@/features/system/services/mock-system-service';

// Replace this export with an HTTP-backed implementation when the NestJS API is ready.
export const systemService = mockSystemService;
